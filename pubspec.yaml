
name: load_more_wrapper
description: A stateless and reusable Flutter widget to add load-more functionality to scrollable widgets.
version: 6.0.0
homepage: https://github.com/ahmedelmwafy/load_more_wrapper
environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

executables:
  load_more_wrapper: load_more_wrapper


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
